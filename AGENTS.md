# AGENTS.md

## Cursor Cloud specific instructions

This repository is currently an empty/blank project initialized with only a `README.md` file. There is no application code, build system, test framework, or service configuration present.

### Current state
- No source code, package manager files, or dependency manifests
- No Docker/container configuration
- No CI/CD pipeline
- No linting, testing, or build tooling configured

### Development notes
- When application code is added, update this section with instructions for running, testing, and linting the project.
- The update script (`echo "No dependencies to install"`) is a placeholder and should be replaced once a package manager or dependency file is introduced.
